WIFI_SSID = "YOUR_WIFI_SSID"
WIFI_PASSWORD = "YOUR_WIFI_PASSWORD"

# mDNS hostname (access board as HOSTNAME.local)
# Optional - leave as None to use default
HOSTNAME = "esp32"  # Will be accessible as esp32.local